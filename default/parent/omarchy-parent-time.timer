# Runs the screen-time tick once a minute while the machine is up. Timers do
# not fire during suspend, so a sleeping laptop is charged at most one tick.
[Unit]
Description=Omarchy screen-time countdown

[Timer]
OnBootSec=1min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
