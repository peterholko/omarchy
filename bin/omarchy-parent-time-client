#!/usr/bin/env python3
# omarchy:summary=Talk to the screen-time daemon (JSON; the shell and omarchy-parent time use it)
# omarchy:hidden=true
"""The client of omarchy-parent-timed; see lib/screen-time/README.md."""

import os
import sys

sys.path.insert(0, os.path.join(os.environ.get("OMARCHY_PATH") or os.path.dirname(os.path.dirname(os.path.realpath(__file__))), "lib", "screen-time"))

from screen_time.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
